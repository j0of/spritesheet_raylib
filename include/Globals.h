#include <string>

namespace spritesheet {
    namespace Globals {
        const inline int WINDOW_WIDTH = 720;
        const inline int WINDOW_HEIGHT = 600;
        const inline std::string WINDOW_TITLE = "Spritesheet test";
    }
}